import { GetOriginDirective } from './get-origin.directive';

describe('GetOriginDirective', () => {
  it('should create an instance', () => {
    const directive = new GetOriginDirective();
    expect(directive).toBeTruthy();
  });
});
